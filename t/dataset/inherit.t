use strict;
use warnings;
use Test::More;
use Test::Identity;
use Gnuplot::Builder::Dataset;

fail("inheritance. source, options, and data provider");
fail("setting undef to option in child disables the option");

done_testing;
